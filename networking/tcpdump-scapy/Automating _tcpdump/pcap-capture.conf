BASE_FILENAME="capture"
OUTPUT_DIR="/var/log/pcap"
FILE_SIZE_MB=100  # Max file size in MB before rotation
ROTATE_SECONDS=600  # Time in seconds before rotating files
PERMISSION_FILE="/etc/pcap-permission"
